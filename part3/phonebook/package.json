{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "deploy": "fly deploy",
    "build:ui": "@powershell Remove-Item -Recurse -Force dist; Set-Location ../../part2/phonebook/; npm run build; Copy-Item -Recurse dist ../../part3/phonebook",
    "deploy:full": "npm run build:ui && npm run deploy --depot=false",
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.7.10"
  }
}
